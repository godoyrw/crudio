{
  "ConnectionStrings": {
    "PostgresConnection": "Host=localhost;Port=5432;Database=${POSTGRES_DB};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}",
    "MongoConnection": "mongodb://${MONGO_ROOT_USERNAME}:${MONGO_ROOT_PASSWORD}@localhost:27017",
    "RedisConnection": "localhost:6379,password=appuser"
  },
  "MongoDatabase": "CrudIoLogs",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}